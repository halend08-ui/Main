# research-engine

Python package implementing the investment research engine.
See the repository root `README.md`, `ARCHITECTURE.md` and `DEVELOPMENT.md` for full documentation.

Quick start:

    cd engine
    pip install -e ".[dev]"
    python -m pytest
    python -m research_engine.cli --help
