# Runtime core (required)
numpy>=1.24
PyYAML>=6.0

# Live data providers (optional; needed only for network ingestion)
requests>=2.31

# HTTP API for the dashboard (optional)
fastapi>=0.110
uvicorn>=0.27

# Development / test
pytest>=7.4
pytest-cov>=4.1
